{
  "version": 1,
  "experiments": [
    {
      "id": "chitubox-import",
      "name": "Chitubox File Import",
      "description": "Enable importing .chitubox project files. Lacks comprehensive testing and may not work with all files.",
      "defaultEnabled": false,
      "gatedPlugins": ["chitubox-import"]
    },
    {
      "id": "auto-supports",
      "name": "Auto Supports",
      "description": "Automatic support generation from model analysis. Early access.",
      "defaultEnabled": false
    },
    {
      "id": "auto-rotation",
      "name": "Auto Orientation",
      "description": "Orientation suggestions that minimize support contact area. Early access.",
      "defaultEnabled": false
    },
    {
      "id": "model-ao",
      "name": "Baked Model Occlusion",
      "description": "Ambient occlusion baked once per model on a background thread and shaded by the model material. No per-frame cost; the bake runs after a model loads.",
      "defaultEnabled": false
    }
  ]
}
